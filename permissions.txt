dir: rwx
      \\\
       \\*- cd + use
        \*-- create stuff (requires x)
         *--- ll

to use (read, exe, write...) a file we need x on ALL parent dirs
it's different for modifying a dir; w is only needed on the actual dir being changed
